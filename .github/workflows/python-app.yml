--- a/.github/workflows/tasukeru-analysis.yml
+++ b/.github/workflows/tasukeru-analysis.yml
@@
 name: Auto-Tasukeru CI
@@
 on:
   pull_request:
     types: [opened, synchronize, reopened]
     paths:
       - "**/*.py"
       - "**/*.ipynb"
       - "pyproject.toml"
       - "requirements*.txt"
       - ".github/workflows/tasukeru-analysis.yml"
@@
 jobs:
-  analyze:
+  analyze:
     runs-on: ubuntu-latest
     timeout-minutes: 10
-    # forkからのPRでは外部送信しない（情報漏えい対策）
-    if: ${{ github.event.pull_request.head.repo.fork == false }}
 
     steps:
       - name: Checkout code
         uses: actions/checkout@v4
         with: { fetch-depth: 1 }
@@
-      - name: Call Tasukeru API
+      # fork PR は外部送信せず、理由をログに残してスキップ
+      - name: Skip external analysis on fork
+        if: ${{ github.event.pull_request.head.repo.fork == true }}
+        run: |
+          echo "::notice title=Policy::Fork PRs are not sent to external analysis."
+          echo "Fork PR detected. External call is intentionally skipped by policy."
+
+      - name: Call Tasukeru API
+        if: ${{ github.event.pull_request.head.repo.fork == false }}
         env:
           TASUKERU_URL:   ${{ secrets.TASUKERU_URL }}   # 例: https://api.example.com/analyze
           TASUKERU_TOKEN: ${{ secrets.TASUKERU_TOKEN }}
         run: |
           set -euo pipefail
           # 30秒タイムアウト + 2回リトライ（合計3回）
           for i in 1 2 3; do
             curl --fail --show-error --silent \
                  --max-time 30 \
                  -X POST "$TASUKERU_URL" \
                  -H "Authorization: Bearer $TASUKERU_TOKEN" \
                  -F "file=@source.zip" \
                  -o report.md && break || sleep 3
           done
           # 念のため、空出力は失敗扱い
           test -s report.md
@@
-      - name: Post report as PR comment
+      - name: Post report as PR comment
+        if: ${{ github.event.pull_request.head.repo.fork == false }}
         uses: peter-evans/create-or-update-comment@v4
         with:
           issue-number: ${{ github.event.pull_request.number }}
           body-path: report.md
@@
-      - name: Upload report artifact (optional)
-        if: always()
+      - name: Upload report artifact (optional)
+        if: ${{ github.event.pull_request.head.repo.fork == false || always() }}
         uses: actions/upload-artifact@v4
         with:
           name: tasukeru-report
           path: report.md
-
-  # fork PRは“外部送信なし”のサニティチェックだけ（任意）
-  dry-run:
-    runs-on: ubuntu-latest
-    timeout-minutes: 5
-    if: ${{ github.event.pull_request.head.repo.fork == true }}
-    steps:
-      - uses: actions/checkout@v4
-        with: { fetch-depth: 1 }
-      - run: echo "Fork PR: external analysis is skipped by policy."
+```
[/BODY]

[FOOTER]
-- END PART 2/2 --
[/FOOTER]
